# Caddy 反代模板（lite 部署用）。
# 用法：复制为 Caddyfile，把域名换成你的，确保 DNS 已指向本机、80/443 已放行，
# Caddy 会自动申请并续期 Let's Encrypt 证书。
#
#   cp Caddyfile.example Caddyfile   # 然后编辑域名

app.your-domain.com {
    reverse_proxy web:3000
}

api.your-domain.com {
    reverse_proxy api:8000
}
