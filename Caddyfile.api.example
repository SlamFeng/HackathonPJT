# Caddy 反代模板（前端 Vercel + 后端本机 的「后端」用）。
# 只反代 API（前端在 Vercel，不在这台机器上）。
# 用法：复制为 Caddyfile.api，把域名换成你的 API 子域名；确保 DNS 已指向本机、80/443 已放行，
# Caddy 会自动申请并续期 Let's Encrypt 证书。
#
#   cp Caddyfile.api.example Caddyfile.api   # 然后编辑域名

api.your-domain.com {
    reverse_proxy api:8000
}
